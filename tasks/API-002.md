# API-002: POST /api/sites/remove

## Description

Add REST endpoint to remove a site.

## Requirements

POST /api/sites/remove

Body:
```json
{"domain": "example.com"}
```

Response:
```json
{"success": true, "data": {"message": "Site removed"}}
```

Uses SiteRemoveOperation internally.
